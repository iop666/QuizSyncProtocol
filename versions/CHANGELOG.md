# 协议变更记录（CHANGELOG）

> **本文件是协议版本变更的唯一记录**：协议版本号只在这里声明一次，规范正文引用它、不重复定义。
> 采用 Keep a Changelog 风格；**版本号只写协议版本**，与应用版本无关。
> 本文件**不写日期**：没确认过的日期不写，用「v1 冻结」「未冻结」这类描述代替。

## 0. 改协议时必须同时做的三件事

| # | 做什么 | 在哪里 | 为什么 |
|---|---|---|---|
| 1 | 改规范 | 对应域的规范正文（MUST / SHOULD + 字段表 + 数值表） | 规范是纸面契约，三端都照它实现 |
| 2 | 加 / 改向量 | 一致性向量（能给自动化步骤就给；给不出就先写人工检查项） | 「语义变更没有对应向量」是评审红线，向量是实现的唯一裁判 |
| 3 | 记这里 | 本文件的 `Unreleased` 小节 | 协议版本只能有一处声明 |

> 三件事**同一次提交**完成，缺一视为未完成。冻结主版本 = 打 tag；冻结后**只允许追加**。

## 1. 协议版本与应用版本的关系

| | 协议版本 | 应用版本 |
|---|---|---|
| 回答什么 | 两端**能否正确对话** | 这是**哪个发布包** |
| 形态 | `MAJOR.MINOR.PATCH`（本文件从 `1.0.0` 起记录） | 产品发布号，各端各自走 |
| 出现在哪 | 设备自述、配对响应、WS `hello`；v2 起还有协商头与路径前缀 | 请求头 `X-QS-Client-Version`、响应头 `X-QS-Server-Version`、body 的 `app_version` |
| 谁改它 | 改线缆契约才改 | 发版就改 |
| 现状的错 | 协商**比的是应用版本**的主版本（见 `1.0.0` 的 Changed 与 `Unreleased` 的 C1/C3） | —— |

- **MUST NOT** 用应用版本判断线缆兼容性；**MUST NOT** 把协议版本当发布号用。
- 换实现**不动**协议版本；扩协议**不必然**发应用版本。
- 协商头只取 `MAJOR.MINOR`；HTTP 路径只取 `MAJOR`。路径里出现的主版本与协商头里的主版本**必须一致**。
- **待确认**：版本号形态在本仓库内有两处口径 —— 一处写 `MAJOR.MINOR`，一处写 tag `vX.Y.0`。本文件按 `MAJOR.MINOR.PATCH` 记录（与 `1.0.0` 的起始要求一致），协商头与路径各取所需前缀。

---

## [Unreleased] — v2 目标（`2.0.0` 线，**未冻结**）

v2 是**新主版本**：允许删除、允许改语义，但主线必须同时提供 v1 兼容子集（A25）。
下列 61 条中，**7 条是待决策**（C28–C34），其余是已定方向；未落地的一律**不得**写成已实现。

### Added（27 条）

| # | 新增 | 来源 |
|---|---|---|
| A1 | 主版本进 URL：`/api/v2/*`；同主版本内**只增不改不删** | `10-versioning` |
| A2 | 协商头 `X-QS-Protocol: <major>.<minor>`，请求与响应都带 | `10-versioning` |
| A3 | 请求头 `X-QS-Request-Id`（响应原样回带，用于排障与幂等观测） | v2 计划 §4.4 |
| A4 | `GET /api/v2/info` 给出 `protocol{min,max}` 可机读区间 | `10-versioning` |
| A5 | `limits` 上限对象（多页上限、单图字节上限、上传速率），使上限不再靠两端各自硬编码 | `11-capabilities` |
| A6 | 能力变更事件：能力集合或 AI 配置变化时推送给已连接客户端 | `11-capabilities` |
| A7 | WS 信封升级为 `{v, type, id, ts, corr, payload}`（`v` = 协议主版本） | v2 计划 §4.4 |
| A8 | 新增 WS 事件：配对码变更、Provider 上线、Provider 下线 | v2 计划 §4.4 |
| A9 | 服务端 → 客户端的 `ops` 事件**真正实现**（v1 只在旧契约里列过，实现从未发出） | `05-websocket` |
| A10 | Provider 派发事件：派发给 Provider 的任务、Provider 回传的结果 | v2 计划 §4.4 + `08-tasks` |
| A11 | 新增取消端点，使 `cancelled` 成为三端都能产生的状态 | `08-tasks` |
| A12 | 新增 `403 forbidden`（角色越权；v1 不存在该码） | `03-auth-pairing` / `09-errors` |
| A13 | 错误体新增 `retryable`、`request_id`、`details` 字段（可机读信息不再埋进 `message`） | `10-versioning` |
| A14 | 错误码的 `deprecated` 标记与替代码字段 | `09-errors` |
| A15 | 字段的 `deprecated_since` 标记 | `10-versioning` |
| A16 | `503 ai_not_configured`：未配置 AI 时创建任务**前置**拒绝，带 `retry_after_seconds` | `04-http-api` / `11-capabilities` |
| A17 | `409 invalid_state`：对非 `failed` 的任务调用重试 | `04-http-api` / `08-tasks` |
| A18 | 令牌到期时间列与显式的令牌刷新流程 | `03-auth-pairing` |
| A19 | 主机专属的重签密钥端点（`rekey`） | v2 计划 §4.1 |
| A20 | 分块 / 断点续传上传（创建上传会话 → 分块 → 提交），一次性上传作为兼容子集保留 | `07-images` |
| A21 | 设备自述响应暴露**实际生效端口**（加法字段；现状端口只从二维码 / 手输获得） | `02-transport` |
| A22 | 大 JSON（操作日志批量、快照）支持 `Content-Encoding: gzip` | `02-transport` |
| A23 | 大 JSON 改为 NDJSON 流式分页（去掉「整个快照一次性 JSON」这个无上限端点） | `02-transport` |
| A24 | AI 调用的幂等键（任务 id + 尝试次数），修「超时重发可能重复计费」 | v2 计划 §4.5 |
| A25 | **v1 兼容层**：v2 主机必须同时提供 `/api/v1/*` 兼容子集（含 5 条逐字语义：活跃任务带会话、本机任务不暴露任务 id 且两者相等、合集删除方向不对称、缓存与同图去重只对单页、多页硬上限 6） | `10-versioning` |
| A26 | 所有 JSON 端点的请求体体积上限（默认 1 MiB；操作日志批量 8 MiB；超限 413） | `02-transport` |
| A27 | `capabilities` 改为按能力**计算**得出（不再是不随配置变化的常量数组） | `10-versioning` / `11-capabilities` |

### Changed（34 条）

| # | 变更 | 来源 |
|---|---|---|
| C1 | 协商依据改为**协议版本**（不再比应用版本的主版本） | `10-versioning` |
| C2 | `X-QS-Client-Version` / `X-QS-Server-Version` 降为**纯诊断**信息 | `10-versioning` |
| C3 | 不兼容响应由 `426 version_mismatch` 改为 `426 protocol_mismatch`，且对**所有**请求生效（含不带协商头的：按最低支持版本处理，不再一律放行） | `10-versioning` |
| C4 | 吊销不存在的设备：由恒回 200 改为 `404` | `04-http-api` |
| C5 | 主机专属端点（吊销设备、快照、设备列表等）引入角色校验；吊销只允许主机或该设备自己 | `03-auth-pairing` / v2 计划 §4.1 |
| C6 | 任务重试由「恒回固定字面量 `queued`」改为返回任务**真实状态** | `04-http-api` / `08-tasks` |
| C7 | 重新识别响应补任务 id；「会话无页序」由未捕获的 500 改为协议错误（400 或 409） | `04-http-api` |
| C8 | 主机本机识别由「不经任务队列」改为**统一入队**（有真实任务 id、可查、可重试）；活跃任务探测退化为**纯探测** | `08-tasks` |
| C9 | 主机自发起任务的 `task_id` 与 `session_id` 由「恒相等」改为**独立生成**（v1 兼容层负责映射回相等） | `01-conventions` / `08-tasks` |
| C10 | 设备标识由固定字面量改为**每安装生成一次并持久化**的 UUIDv4 | `01-conventions` / `03-auth-pairing` |
| C11 | 设备标识改为**每台客户端独立**、服务端支持多台在册；超上限用明确错误码拒绝，不再静默顶掉旧设备 | `03-auth-pairing` |
| C12 | 配对码由「有效期内可反复使用」改为**一次性** | `03-auth-pairing` |
| C13 | 图片上传时**真算**宽高（不再恒为 `null`） | `07-images` |
| C14 | 全量快照里的图片 / 设备 / 合集由「每次全量」改为按水位**可增量、可分页**；`has_more` 覆盖全部集合；本地专属列从同步载荷中剔除 | `04-http-api` / `07-images` |
| C15 | 操作日志拉取的 `since_lamport` 与 `cursor` 收敛为**单一游标参数** | `04-http-api` |
| C16 | HTTP 自动重试落到规范：只对 GET 与幂等 POST，退避 1s / 3s / 10s；4xx（除 408 / 429）不重试 | `02-transport` |
| C17 | 离线队列收紧：只有传输层失败、`429` 与 `5xx` 入队；4xx 直接失败并提示 | `09-errors` |
| C18 | 服务端**校验** `pong`，90 秒断开死连接（现状：不校验、不断开；客户端侧 40 秒静默判死不变） | `02-transport` / `05-websocket` |
| C19 | 超时：上传单独 120 秒；AI 调用 90 秒 | `02-transport` |
| C20 | 客户端只在吊销事件的设备 id **等于自己**时清本地配对（现状：连接层比对、消息落地层不比对） | `05-websocket` |
| C21 | 客户端按能力名**降级**：缺多页能力只发单页、缺合集能力不发合集 id、缺同步能力仅轮询、缺取图能力不传图；主机不得声明自己不具备的能力；两端忽略不认识的能力名 | `11-capabilities` |
| C22 | 能力与版本绑定：收到 426 必须停止请求，**不得猜测降级** | `11-capabilities` |
| C23 | `code_expired` 的 `retry_after_seconds` 由「已过期秒数」改为「需主机刷新配对码」，或直接省略该字段 | `09-errors` |
| C24 | 两套主机实现的差异收敛：同场景同码同状态（重新识别时原图缺失）；本机控制面端点不计入协议错误码 | `09-errors` |
| C25 | 图片改为「文件 + 内容哈希」，执行方与客户端本地都不做内存常驻 | v2 计划 §4.2 |
| C26 | 上传限流（30 次 / 分钟 / 设备）的**数值与粒度明确不变**（v2 冻结） | `02-transport` |
| C27 | 传输形态与「局域网明文、不做 TLS、用吊销兜底」**明确不变**（v2 冻结） | v2 计划 §4.1 |
| C28 | **待决策**：`cached` 同时存在布尔与整数两种形态，统一为哪一种 | `01-conventions` |
| C29 | **待决策**：上传响应用 `image_hash`、图片实体用 `hash`，字段名是否统一 | `01-conventions` |
| C30 | **待决策**：重试后任务行与会话行的错误字段是否一起清（现状会出现「无错误码 + 有错误信息」） | `08-tasks` |
| C31 | **待决策**：客户端 → 服务端的 `ack` / `push_ops` 两条路径**接上还是删除**（现状生产路径从不发） | `05-websocket` |
| C32 | **待决策**：操作日志折叠的阈值口径与「跨设备删 op」是否收紧 | `06-sync` |
| C33 | **待决策**：是否允许「吊销可被设备用重新配对自助解除」 | `03-auth-pairing` |
| C34 | **待决策**：「用户手改答案 / 解析」机制是补界面入口还是整体废弃（现状只有仓库层调用，界面无入口） | `06-sync` |

### Deprecated / Removed（10 条派生条目，不重复计入上面的 61 条）

| # | 类别 | 条目 | 依据 |
|---|---|---|---|
| D1 | 标废 | 用**应用版本**协商与据此产生的 `426 version_mismatch` 口径 | C1 / C3 |
| D2 | 标废 | 整包 JSON 的大响应（操作日志批量、快照的 `limit` / `offset` 形态）；兼容层继续提供 | A23 / C14 |
| D3 | 标废 | 操作日志拉取的双游标参数中的一个（`since_lamport` 与 `cursor`） | C15 |
| D4 | 标废 | 任务重试恒回固定字面量 `queued` 的语义 | C6 |
| D5 | 标废 | 客户端 → 服务端的 `ack` / `push_ops`（若决定不接上则标废，见 C31） | C31 |
| R1 | 移除 | 未配 AI 时「先回 202、再必然失败」的执行路径 | A16 |
| R2 | 移除 | 重新识别的「会话无页序 → 未捕获异常」路径 | C7 |
| R3 | 移除 | 活跃任务里「任务 id 恒等于会话 id」的约定（由 v1 兼容层映射） | C9 |
| R4 | 移除 | 两套主机实现造成的「同场景两个不同取值」重复码 | C24 |
| R5 | 移除 | 重试对非失败任务「返回 200 但什么都不改」的行为 | C6 |

> 移除**只在 v2 这个新主版本里允许**；在已冻结的主版本里，上面 10 条只能标废、不能移除。

---

## [1.0.0] — v1 冻结

**冻结范围**：把已发布的行为**照原样**写成规范（按实现反推），包括所有看起来不干净但已被接受的不对称语义。v1 不是设计，是记录；写它的时候不许顺手改良。

### Added

- 端点表（17 条）与路径前缀恒为 `/api/v1/`。
- 协商现状：请求头 `X-QS-Client-Version`（应用版本）；主机比较两端**应用版本的主版本**，字符串相等才放行；不一致回 `426 version_mismatch`；**完全不带该头一律放行**；**值不可解析也回 426**；检查顺序是**先鉴权、后版本**，且版本检查在**路由之前**；`/ws` **不参与**协商。
- 响应头 `X-QS-Server-Version`（所有 `/api/*` 响应，含错误；`/ws` 除外）。
- 协议版本自述：整数 `1`，在设备自述、配对响应、WS `hello` 三处同源；**没有任何一端依据它分支**。
- 能力自述：恒 5 项（分析、同步、取图、合集、多页），顺序不是契约，客户端**不据此分支**。
- 错误体三键 `{code, message, retry_after_seconds}`，该键恒在。
- 硬约束：单图 2 MiB、多页上限 6、上传 30 次 / 分钟 / 设备、配对码 5 分钟、5 次 / 分钟、连续 10 次失败锁 60 秒。

### Changed（相对旧契约文本的修正，全部按实现反推）

- `426` 的 body 里**没有**独立的版本字段：双方版本只出现在 `message` 文本里。
- 「除配对外的所有请求都要 token」不成立：设备自述同样免鉴权。
- 「主版本不一致」隐含只在带了头时判定：实现明确对**缺头请求放行**。
- 版本头「覆盖所有请求」不成立：`/ws` 完全绕过中间件，既无检查也无响应头。
- 端点、字段与数值若干处按实现补齐（队列深度上限、`cached`、`local_path` 等）。

### Removed

- 无。`1.0.0` 是首个冻结版本，此处不删任何东西；协议已发布的错误码**永不删除**，只能标记废弃。

## 版本记录格式（后续维护者照抄）

未冻结的版本用 `## [Unreleased] — <目标主版本线>`，冻结的版本用 `## [X.Y.Z] — <冻结范围一句话>`；两者都用 `### Added / Changed / Deprecated / Removed` 四个小节（没有内容的小节写「无」）。每条一行：做了什么 + 依据哪份规范；待决策的显式标注。
